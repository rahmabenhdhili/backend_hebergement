spring:
  datasource:
    url: jdbc:mysql://localhost:3306/your_db?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC
    username: username
    password: "password"

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true