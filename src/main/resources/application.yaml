spring:
  datasource:
    url: jdbc:mysql://localhost:3306/you_db?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC
    username: ""
    password: ""

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

